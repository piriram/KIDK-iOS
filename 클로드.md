# Claude 작업 가이드

이 문서는 Claude와 협업할 때 따라야 할 프로젝트 규칙을 정의합니다.

## 📋 TODO 관리 규칙

### TODO 발견 시
- **새로운 TODO를 발견하거나 생성할 때는 반드시 `.docs/TODO.md` 파일에 기록할 것**
- 코드의 `// TODO:` 주석은 임시 메모용으로만 사용
- 실제 작업 추적은 `.docs/TODO.md`에서 관리

### TODO 파일 위치
```
.docs/TODO.md  (Git에서 제외됨)
```

### TODO 작성 형식
```markdown
### [작업 제목]
**파일**: `경로:라인번호`
**설명**: 상세 설명
**우선순위**: 높음/중간/낮음
**상태**: 미착수/진행중/완료

**관련 작업**:
- [ ] 세부 작업 1
- [ ] 세부 작업 2
```

## 🔧 개발 환경

### 빌드 환경
- **Debug**: 개발 환경 (Mock 데이터 사용 가능)
- **Release**: 배포 환경 (실제 API만 사용)

### API 설정
- 설정 파일: `KIDK/Config/Secrets.plist`
- 환경 관리: `AppConfiguration.swift`

## 📂 프로젝트 구조

```
KIDK/
├── Data/
│   ├── Network/         # API 레이어
│   ├── Repository/      # Repository 구현
│   └── Entity/          # Realm 엔티티
├── Domain/
│   ├── Model/           # 도메인 모델
│   └── Interface/       # Repository 프로토콜
├── Presentation/
│   ├── View/            # ViewController
│   ├── ViewModel/       # ViewModel (RxSwift)
│   └── Coordinator/     # 화면 전환 관리
└── Core/
    ├── DesignSystem/    # 디자인 시스템
    └── Util/            # 유틸리티
```

## 🎯 아키텍처 원칙

- **MVVM + Coordinator**: 화면 전환과 비즈니스 로직 분리
- **Repository Pattern**: Mock/Real API 전환 용이
- **RxSwift**: 리액티브 프로그래밍
- **Protocol-Oriented**: 확장 가능한 설계

## 🚫 주의사항

- `Secrets.plist`와 `GoogleService-Info.plist`는 절대 커밋하지 말 것
- 헤더 주석은 삭제됨 (이미 제거 완료)
- Mock 데이터는 개발 환경에서만 사용
- 모든 네트워크 에러는 NetworkError로 통일

## 📝 커밋 컨벤션

- `Feat:` 새로운 기능
- `Fix:` 버그 수정
- `Refactor:` 리팩토링
- `Chore:` 설정/빌드 관련
- `Docs:` 문서 수정
- `Design:` UI/UX 변경

## 🔄 브랜치 전략

- `main`: 안정 버전
- `feature/*`: 새 기능 개발
- `fix/*`: 버그 수정
- `claude/*`: Claude 작업용 임시 브랜치
