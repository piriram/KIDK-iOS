# KIDK Receipt OCR - 남은 TODO

작성일: 2026-03-01
브랜치 기준: `feat/receipt-ocr-autofill`

## 배경
Vision 기반 영수증 OCR 자동 입력 기능이 1차 구현되었고, Debug 빌드 및 스모크 검증까지 완료됨.
현재 문서는 프로덕션 품질로 올리기 위한 후속 작업을 정리한다.

---

## 1) 테스트 체계 정식화 (우선순위: 높음)

### 현재 상태
- `ReceiptScanViewController` 내부 `#if DEBUG` 스모크 테스트(assert)만 존재
- XCTest 타깃 기반 자동화 테스트 부재

### TODO
- `KIDKTests`(또는 기존 테스트 타깃)에서 OCR 파서 단위 테스트 추가
- 케이스 확장:
  - 키워드 인접 금액 파싱 정상 케이스
  - 키워드 누락 시 최대값 fallback 케이스
  - `1,000` vs `10,000` 혼동 케이스
  - 전화번호/주소가 상호명 후보로 섞이는 케이스
  - 한/영 혼합 영수증 케이스
- 회귀 테스트 데이터셋(실영수증 샘플 텍스트 fixture) 관리

### 완료 기준
- 핵심 파싱 규칙에 대한 XCTest 15개 이상 작성
- CI/로컬에서 테스트 재현 가능

---

## 2) 금액 보정 규칙 정교화 (우선순위: 높음)

### 현재 상태
- `x10` 상향 보정 규칙으로 1,000/10,000 혼동 완화
- 일부 케이스에서 과보정 가능성 존재

### TODO
- 보정 로직에 confidence/거리/통화표기 가중치 추가
  - 키워드와의 라인 거리
  - `원`, `KRW`, `₩` 표기 여부
  - 소수점/세금 라인(`부가세`, `VAT`) 분리
- 상향/하향 보정 규칙을 score 기반으로 통합
- 임계값과 룰을 상수화하여 튜닝 가능하게 구조화

### 완료 기준
- 오보정 재현 케이스에서 기대값 통과
- 금액 오인식 주요 케이스(특히 x10 오류) 유의미하게 감소

---

## 3) OCR 전처리 튜닝 (우선순위: 중간)

### 현재 상태
- 문서 카메라 경로에서 고정 노출/대비 보정 적용

### TODO
- 전처리 파라미터 실샘플 기반 튜닝
- 다중 필터 전략 실험:
  - 원본
  - 대비 보정
  - 노이즈 감소
- 후보 결과 중 confidence/파싱 성공률 기반 선택

### 완료 기준
- 반사/구김/저조도 샘플에서 인식 성공률 개선
- 파라미터 변경 근거(샘플/비교표) 문서화

---

## 4) UX 검증 강화 (우선순위: 중간)

### 현재 상태
- 자동 입력 후 사용자 확인 버튼을 통한 저장 게이트 적용 완료

### TODO
- 안내 문구/상태 라벨 가독성 개선
- 필수값 누락 시 에러 메시지 명확화
- 실패 시 수동 입력 전환 UX(버튼/포커스/힌트) 미세 조정

### 완료 기준
- 잘못된 자동입력 저장 사례 재현 불가
- 수동 fallback 사용성 확인(내부 QA)

---

## 5) 관측 지표(운영 관점) 추가 (우선순위: 낮음)

### TODO
- OCR 성공/실패, 사용자 수정 여부, 저장 완료율 로깅
- 입력 시간 지표(자동+확인 vs 수동) 추적

### 완료 기준
- 실제 사용자 환경에서 개선 효과 재측정 가능

---

## 권장 진행 순서
1. 테스트 체계 정식화
2. 금액 보정 규칙 정교화
3. 전처리 튜닝
4. UX 검증 강화
5. 운영 지표 추가
